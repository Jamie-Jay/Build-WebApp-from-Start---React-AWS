import React from 'react';

const Landing = () => (
  <div>
    <h1>Friends</h1>
  </div>
);
// The landing page is your default route (e.g. http://yourdomain/). That's the place where a user ends up when visiting your web application. The user doesn't need to be authenticated to go this route.
export default Landing;
